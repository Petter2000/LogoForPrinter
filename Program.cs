using System.Text;
using System;
using LogoForPrinter.Model;

namespace LogoForPrinter // Note: actual namespace depends on the project name.
{
  public class Program
  {
    static void Main(string[] args)
    {
      //WorksheetOffsets.FromTextToImage();
      WorksheetOffsets.FromImageToText();
    }
  }
}
